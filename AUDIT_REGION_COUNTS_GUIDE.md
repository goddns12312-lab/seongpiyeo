# 🔍 지역별 감사 스크립트 (audit-region-counts.js)

## 📋 개요

사이트의 매물 수와 DB의 저장 수를 비교하여 누락된 매물을 확인하는 감사 도구입니다.

**현재 발견 사항:**
- 강원도(강원): 사이트 16개 vs DB 12개 (4개 누락, 75% 저장률)
- 제주도(제주): 사이트 6개 vs DB 1개 (5개는 이미지 없음으로 스킵, 정상)

## 🚀 사용법

### 1️⃣ 모든 지역 감사 (빠른 모드)
```bash
node scripts/audit-region-counts.js
```
결과: 모든 지역의 사이트 vs DB 개수 비교

### 2️⃣ 특정 지역만 감사
```bash
node scripts/audit-region-counts.js --region=강원도
node scripts/audit-region-counts.js --region=경기도
```

### 3️⃣ 상세 모드 (누락 매물 목록)
```bash
node scripts/audit-region-counts.js --region=강원도 --detailed
```
결과: 각 게시글별 이미지 확인 + 누락된 매물 idx/제목/사유

### 4️⃣ DB만 분석 (크롤링 없음)
```bash
node scripts/audit-region-counts.js --skip-crawl
```
결과:
- region 매핑 오류
- 중복 idx
- 이미지 없는 매물 개수
- status 분포

## 📊 출력 형식

### 빠른 모드 예시
```
📍 강원도 (2 page)
  강원도 - 2/2 페이지: 0개
  강원도 - 1/1 페이지: 16개
  사이트 총 게시글: 16개
  DB 저장됨:       12개
  차이:             4개 ⚠️

📊 최종 요약
  사이트 총계: 16개
  DB 저장:     12개
  총 누락:      4개
  저장률:       75.0%
```

### 상세 모드 예시 (제주도)
```
📍 제주도 (1 page)
  제주도 - 1/1 페이지: 6개
  상세 정보 수집 중...
  사이트 총 게시글: 6개
  DB 저장됨:       1개
  차이:             5개 ⚠️

  이미지 없는 게시글: 5개 (스킵됨)
  예상 업로드 수:     1개
  실제 저장 수:       1개

  누락 매물 (DB에 없는 것): 5개
    - idx=167085118 | 서귀포시... | 사유: 이미지 없음
    - idx=22853747  | 매매... | 사유: 이미지 없음
    ...

  이미지 없어 스킵됨 (정상): 5개
    - idx=167085118 | 서귀포시...
    - idx=22853747  | 매매...
```

## 🔧 주요 기능

### 빠른 모드 (기본)
- ✅ 각 지역의 모든 페이지 순회
- ✅ 목록 페이지의 게시글 개수 세기 (`li.tit a.title_link`)
- ✅ DB의 저장 개수와 비교
- ✅ 실행 시간: 30초~2분 (지역 수에 따라)

**사용 시기**: 빠르게 누락 여부만 확인할 때

### 상세 모드 (`--detailed`)
- ✅ 각 게시글의 상세페이지 방문
- ✅ 게시글 idx 추출 (URL에서)
- ✅ 이미지 개수 확인 (`img.fr-dii._img_light_gallery`)
- ✅ 누락된 idx 식별 (DB와 비교)
- ⚠️ 현재: 페이지 로드 문제로 불안정 (개선 예정)

**사용 시기**: 누락된 매물의 idx/제목을 파악해야 할 때

### Skip-Crawl 모드 (`--skip-crawl`)
- ✅ 크롤링 없이 DB만 분석
- ✅ region 매핑 오류 검출
- ✅ 중복 idx 검출
- ✅ 이미지 없는 매물 통계
- ✅ status 분포

**사용 시기**: 빠르게 DB 오류만 확인할 때

## 📈 DB 오류 분석

모든 모드에서 자동으로 실행되는 분석:

```
📊 DB 오류 분석

  region 매핑 오류: 0 개
  (region 필드가 REGIONS에 없는 항목)

  중복 idx: 0 개
  (같은 idx가 2개 이상인 경우)

  이미지 없는 매물: 437 개
  (listing_images 테이블에 연결되지 않음)

  status 분포:
    - active: 639개
    - pending: 0개
    - hidden: 0개
```

## 🔍 해석 가이드

### "차이"가 양수인 경우 (⚠️)
```
사이트 총 게시글: 16개
DB 저장됨:       12개
차이:             4개 ⚠️
```
→ 4개의 매물이 사이트에는 있지만 DB에 없음
- 이미지 없는 매물일 가능성 높음 (스킵됨, 정상)
- 최근에 올라온 매물일 가능성 (아직 크롤링 안 됨)
- 크롤링 실패한 매물일 가능성 (오류 발생)

### "차이"가 0인 경우 (✅)
```
차이:             0개 ✅
```
→ 사이트의 모든 매물(이미지 있는 것)이 DB에 저장됨

### "차이"가 음수인 경우 (⚠️)
```
사이트 총 게시글: 10개
DB 저장됨:       15개
차이:           -5개 ⚠️
```
→ DB에 더 많은 매물이 있음
- 사이트에서 삭제된 매물이 DB에 남아있음
- 데이터 정합성 오류

## 📊 현재 상태 (2026-05-26)

| 지역 | 사이트 | DB | 차이 | 저장률 | 상태 |
|-----|--------|-----|------|--------|------|
| 강원도 | 16 | 12 | 4 | 75% | ⚠️ 확인 필요 |
| 제주도 | 6 | 1 | 5 | 17% | ✅ 정상 (이미지 부족) |
| 서울 | ? | 49 | ? | ? | ⏳ 테스트 필요 |
| 경기도 | ? | 0 | ? | ? | ⏳ 테스트 필요 |

## 🛠️ 다음 단계

### 강원도 4개 누락 원인 파악
1. 상세 모드로 누락된 idx 확인 (현재 불안정)
2. 해당 idx의 상세페이지 직접 방문하여 이미지 확인
3. 크롤러 로그에서 스킵 사유 확인

### 상세 모드 안정성 개선
- [ ] 페이지 로드 타임아웃 증가
- [ ] 클릭 대신 URL 직접 접근
- [ ] 스테일 엘리먼트 처리 개선

### 전 지역 감사
```bash
node scripts/audit-region-counts.js  # 모든 지역 빠른 감사
```

## 🔗 관련 파일

- `scripts/audit-region-counts.js` - 감사 스크립트
- `scripts/region-config.js` - 지역 설정
- `scripts/crawler-state.json` - 크롤러 상태
- `scripts/crawl-regions.js` - 신규글 크롤러

---

**스크립트 완성일**: 2026-05-26
**현재 상태**: ✅ 기본 기능 완성, ⏳ 상세 모드 최적화 필요
