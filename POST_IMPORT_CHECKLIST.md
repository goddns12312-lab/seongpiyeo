# Supabase Import 후 Localhost 검증 체크리스트

## 📋 임포트 전 점검

- [ ] `scripts/output/listings.json` 존재 확인
- [ ] `scripts/output/failed.log` 재시도 항목 확인 및 재시도 완료
- [ ] 검증 결과 확인 (description, images, idx 등)
- [ ] Supabase 기존 status 필드 보존 확인

---

## 🔍 Localhost 페이지 검증

### 1. 매물 목록 페이지 (http://localhost:3001/listings)

**목록 표시 확인:**
- [ ] 매물 목록이 표시되는가?
- [ ] 새로 추가된 매물이 포함되어 있는가?
- [ ] 카드의 썸네일 이미지가 정상 로드되는가?
- [ ] 제목, 지역, 가격 정보가 정상 표시되는가?

**페이지네이션:**
- [ ] 페이지 이동이 정상 작동하는가?
- [ ] 페이지별로 올바른 매물이 표시되는가?

**필터링:**
- [ ] 지역별 필터가 작동하는가?
- [ ] 가격순 정렬이 작동하는가?

---

### 2. 매물 상세 페이지 (http://localhost:3001/listings/[id])

**기본 정보:**
- [ ] 제목이 정상 표시되는가?
- [ ] 지역(region), 구(district)가 정상 표시되는가?
- [ ] 가격 정보가 정상 표시되는가? (보증금, 월세, 권리금)
- [ ] 면적(area_sqm)이 정상 표시되는가?
- [ ] 연락처(contact)가 정상 표시되는가?

**이미지 표시:**
- [ ] 썸네일 이미지가 정상 로드되는가?
- [ ] 메인 이미지가 정상 로드되는가?
- [ ] 여러 이미지일 경우 모두 표시되는가?

**상세 정보:**
- [ ] 📋 게시글 원문 섹션이 표시되는가?
- [ ] 게시글 원문에 HTML 태그가 없고 순수 텍스트만 있는가?
- [ ] 시설정보가 정상 표시되는가?
- [ ] 입주가능일이 정상 표시되는가?
- [ ] 사업자 여부가 정상 표시되는가?

---

### 3. 신규 매물 확인 (pending 상태)

**데이터베이스에서 확인:**
```sql
-- Supabase SQL Editor에서 실행
SELECT idx, title, status, created_at, updated_at 
FROM listings 
WHERE status = 'pending' 
ORDER BY created_at DESC 
LIMIT 10;
```

- [ ] 신규 매물이 `pending` 상태로 저장되었는가?
- [ ] 기존 매물은 이전 `status` 값이 유지되었는가?

**페이지에서 확인:**
- [ ] pending 상태 매물도 목록/상세 페이지에서 조회 가능한가?
- [ ] pending 상태 표시(배지/레이블)가 있는가? (있다면)

---

### 4. 이미지 저장소 확인

**Supabase Storage에서 확인:**
- [ ] `listings` 버킷에 `{idx}/` 폴더들이 생성되었는가?
- [ ] 각 폴더에 이미지 파일들이 저장되었는가? (1.jpg, 2.jpg, ...)
- [ ] 공개 URL이 정상 작동하는가?

**페이지에서 확인:**
- [ ] 이미지들이 정상 로드되는가?
- [ ] 이미지 로드 오류(broken image)가 없는가?

---

### 5. 데이터 무결성 확인

**가격 데이터:**
- [ ] monthly_rent 값이 정상 범위인가? (0~9999)
  - 예: 120 (만원) = 월세 12만원
  - 비정상 예: 120000 (잘못된 계산)
- [ ] deposit, premium 값도 확인

**위치 정보:**
- [ ] region이 올바르게 추출되었는가?
  - location: "강서구화곡동" → region: "서울" ✓
  - location: "수원시권선구" → region: "경기" ✓
- [ ] district/location 필드가 원본 값으로 저장되었는가?

**연락처:**
- [ ] 전화번호가 "010-1234-5678" 형식으로 정규화되었는가?
- [ ] 정규화되지 않은 번호가 있는가?

---

### 6. 성능 및 UX

**로딩 속도:**
- [ ] 매물 목록이 빠르게 로드되는가?
- [ ] 상세 페이지가 빠르게 로드되는가?
- [ ] 이미지 로드가 지나치게 오래 걸리는가?

**모바일 반응형:**
- [ ] 모바일 화면에서 목록이 정상 표시되는가?
- [ ] 모바일 화면에서 상세 페이지가 정상 표시되는가?
- [ ] 이미지가 모바일에서도 정상 로드되는가?

---

## ✅ 최종 확인

- [ ] 모든 검증 항목 확인 완료
- [ ] 데이터 손상 또는 누락 없음 확인
- [ ] 신규/기존 매물 status 구분 정상
- [ ] 임포트 준비 완료

---

## 🔧 문제 발생 시

**이미지가 안 보일 때:**
1. Supabase Storage에 파일이 실제 저장되었는지 확인
2. URL 형식 확인: `{SUPABASE_URL}/storage/v1/object/public/listings/{idx}/{filename}`
3. CORS 설정 확인

**가격이 이상할 때:**
1. `scripts/output/listings.json`에서 원본 값 확인
2. parsePrice 함수 로직 검토
3. 한글 가격 포맷 변환 로직 재확인

**데이터가 저장되지 않을 때:**
1. Supabase 연결 상태 확인
2. `.env.local`의 SUPABASE_SERVICE_ROLE_KEY 확인
3. RLS (Row Level Security) 정책 확인

---

## 📊 임포트 결과

```
✨ 신규 추가: ?개 (status=pending)
🔄 기존 업데이트: ?개 (status 유지)
❌ 실패/스킵: ?개
📋 총합: ?개
```

**위 결과를 기록하고 예상 범위와 비교하세요.**

---

## 🎉 임포트 완료 후

- [ ] 관리자 페이지에서 pending 매물 승인 (필요시)
- [ ] 커뮤니티 게시판 데이터도 초기화할지 검토
- [ ] 배너 이미지 설정
- [ ] 운영 시작
