# 관리자 신규글 크롤링 UI 테스트 체크리스트

## ✅ 구현 완료 항목

- [x] `src/app/api/admin/crawl/route.ts` - 크롤링 API
- [x] `src/app/api/admin/crawler-state/route.ts` - 상태 조회 API
- [x] `src/app/admin/crawl/page.tsx` - 관리자 UI
- [x] `src/app/admin/page.tsx` (수정) - 대시보드 링크
- [x] CLI 로직 유지 (crawl-regions.js 변경 없음)

## 🧪 테스트 단계

### 1단계: 개발 환경 준비
```bash
cd c:\Users\B\Desktop\aass
npm run dev
```
- 포트: http://localhost:3002
- 대기: 개발 서버 시작 완료

### 2단계: 관리자 페이지 접근
```
1. http://localhost:3002/admin 접속
2. 관리자 대시보드 로드 확인
3. "신규글 크롤링" 타일 표시 확인
4. 클릭 → http://localhost:3002/admin/crawl 이동
```

### 3단계: UI 요소 확인
```
[ ] "신규글 크롤링" 제목 표시
[ ] "신규글만 수집 모드 (--new-only)" 뱃지 표시
[ ] "전체 지역 선택" 체크박스 표시
[ ] 지역 드롭다운 표시 (11개 지역)
[ ] "크롤링 시작" 버튼 (비활성화 상태)
[ ] 로그 영역 표시 (비어있음)
```

### 4단계: 단일 지역 크롤링 테스트

#### 4-1: 지역 선택
```
1. 드롭다운에서 "강원도" 선택
2. 확인:
   [ ] 최신 idx 표시: 171446342
   [ ] 누적 크롤링 표시: 12개
   [ ] 마지막 크롤링 시간 표시
   [ ] "크롤링 시작" 버튼 활성화 (골드색)
```

#### 4-2: 크롤링 실행
```
1. "크롤링 시작" 버튼 클릭
2. 확인:
   [ ] 버튼 비활성화 + "크롤링 진행 중..." 텍스트
   [ ] 로그 실시간 표시 시작
   [ ] 로그가 자동 스크롤됨
   [ ] 다양한 색상 로그 표시:
       - 회색: 일반 로그
       - 골드: 상태 메시지
       - 빨강: 에러 (있을 시)
       - 초록: 완료 메시지
```

#### 4-3: 크롤링 완료 확인
```
1. "완료" 로그 표시 확인
2. 확인:
   [ ] "신규 저장" 통계 표시 (초록 배경)
   [ ] "스킵됨" 통계 표시
   [ ] 예상 결과:
       - 강원도 신규글 없으므로 신규 저장: 0개
       - 스킵됨: 1~2개 (유효하지 않은 항목)
   [ ] "크롤링 시작" 버튼 재활성화
```

### 5단계: 전체 지역 크롤링 테스트

#### 5-1: 전체 지역 선택
```
1. "전체 지역 선택" 체크박스 클릭
2. 확인:
   [ ] 드롭다운 비활성화
   [ ] 지역별 상태 정보 미표시
   [ ] "크롤링 시작" 버튼 활성화
```

#### 5-2: 크롤링 실행
```
1. "크롤링 시작" 클릭
2. 확인:
   [ ] 각 지역별 로그 표시
   [ ] 지역 구분선 표시 (========)
   [ ] 모든 11개 지역 처리
   [ ] 기존 항목에서 즉시 중단 (idx <= DB최신)
```

#### 5-3: 최종 통계
```
1. 모든 지역 완료 후
2. 확인:
   [ ] 전체 신규 저장 개수 합계
   [ ] 전체 스킵됨 개수 합계
   [ ] 예상: 대부분의 지역이 이미 크롤링됨
       - 신규 저장: 0~5개
       - 스킵됨: 10~20개 이상
```

### 6단계: 에러 처리 테스트

#### 6-1: 지역 미선택 에러
```
1. 지역 선택 안함 + 전체 지역 선택 안함
2. 확인:
   [ ] "크롤링 시작" 버튼 비활성화
   [ ] 버튼 위에 마우스 올리면 회색
```

#### 6-2: 네트워크 에러 시뮬레이션
```
1. 브라우저 DevTools → Network 탭
2. 온라인 상태에서 크롤링 시작 → 중간에 오프라인 상태로 변경
3. 확인:
   [ ] 에러 로그 표시
   [ ] 버튼 재활성화
```

### 7단계: 상태 갱신 테스트

#### 7-1: 크롤링 후 상태 반영
```
1. 새로운 크롤링 실행 (신규글이 있다고 가정)
2. 완료 후 지역 선택 변경
3. 확인:
   [ ] 새로운 최신 idx 표시
   [ ] 새로운 마지막 크롤링 시간 표시
   [ ] 누적 크롤링 개수 증가
```

### 8단계: CLI와 UI 동시성 테스트

#### 8-1: CLI 실행 중 UI 사용
```
1. CLI에서 크롤링 실행:
   node scripts/crawl-regions.js --region=경기도 --new-only
2. 동시에 UI에서 다른 지역 크롤링 시작
3. 확인:
   [ ] 두 프로세스 독립적으로 실행
   [ ] 상호 영향 없음
```

#### 8-2: UI 실행 중 CLI 사용
```
1. UI에서 크롤링 시작
2. 동시에 CLI 실행:
   node scripts/crawl-regions.js --region=서울 --new-only
3. 확인:
   [ ] 두 프로세스 독립적으로 실행
   [ ] 상호 영향 없음
```

## 📊 예상 결과

### 정상 동작
```
✅ 단일 지역 크롤링: 10~15초 (새 항목 있을 시)
✅ 전체 지역 크롤링: 3~5분 (누적)
✅ 로그 실시간 표시: 지연 없음
✅ 상태 갱신: 완료 후 자동
```

### 에러 케이스
```
❌ 지역 미선택: 버튼 비활성화 (의도적)
❌ 네트워크 에러: 에러 로그 표시
❌ 프로세스 오류: 명확한 에러 메시지
```

## 🔍 검증 기준

| 항목 | 기준 | 상태 |
|------|------|------|
| UI 로드 | 에러 없이 로드 | [ ] |
| 지역 선택 | 11개 지역 표시 | [ ] |
| 크롤링 시작 | 즉시 응답 없음 (스트리밍 시작) | [ ] |
| 실시간 로그 | 1초 이내 표시 | [ ] |
| 크롤링 완료 | 정확한 개수 표시 | [ ] |
| 상태 갱신 | 다음 크롤링 시 반영 | [ ] |
| CLI 호환성 | 기존 동작 유지 | [ ] |

## 📝 테스트 결과 기록

### 테스트 1: 단일 지역 (강원도)
```
날짜: ____
시간: ____
결과: [ ] 통과 [ ] 실패
메모:
```

### 테스트 2: 전체 지역
```
날짜: ____
시간: ____
결과: [ ] 통과 [ ] 실패
메모:
```

### 테스트 3: 에러 처리
```
날짜: ____
시간: ____
결과: [ ] 통과 [ ] 실패
메모:
```

### 테스트 4: CLI 호환성
```
날짜: ____
시간: ____
결과: [ ] 통과 [ ] 실패
메모:
```

## 🚨 알려진 문제

### 없음 (아직)

---

**테스트 날짜**: _____
**테스트자**: _____
**결과**: [ ] 모두 통과 [ ] 일부 실패
